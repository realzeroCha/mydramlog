import { TypeFilterViewProps } from "./types";

const TypeFilterView = ({ props, hook }: TypeFilterViewProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default TypeFilterView;
