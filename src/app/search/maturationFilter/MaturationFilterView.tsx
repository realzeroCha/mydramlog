import { MaturationFilterViewProps } from "./types";

const MaturationFilterView = ({ props, hook }: MaturationFilterViewProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default MaturationFilterView;
