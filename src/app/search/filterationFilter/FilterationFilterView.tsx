import { FilterationFilterViewProps } from "./types";

const FilterationFilterView = ({ props, hook }: FilterationFilterViewProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default FilterationFilterView;
