import { PriceFilterViewProps } from "./types";

const PriceFilterView = ({ props, hook }: PriceFilterViewProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default PriceFilterView;
